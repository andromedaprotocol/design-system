import React from "react";

export const {{componentName}} = React.forwardRef<SVGSVGElement, React.ComponentPropsWithRef<"svg">>(
  ({ color = "currentColor", ...props }, ref) => {
    return (
        <svg fill="currentcolor" stroke={color} {...props} ref={ref}>
          {{#each tags}}
          {{{this}}}
          {{/each}}
        </svg>
    );
  }
);